
------------------------------------- PENDENTES Sg -----------------------------------------------


Desafios Extras:

Criar um endpoint para realizar a busca de artistas atrav�s do id do estilo.
GET /api/estilos/{id}/artistas

Criar um endpoint para realizar a busca de artistas atrav�s do nome do estilo.
GET /api/estilos/buscar/{nome}/artistas

Criar um endpoint para listar a quantidade de estilos dispon�veis.
GET /api/estilos/quantidade

Criar um endpoint para listar a quantidade de estilos e a quantidade de artistas dispon�veis.
GET /api/admin/dashboard


--------------------------------- FEITOS TF ------------------------------------------------


Criar um projeto chamado Senai.Optus.WebApi.

EF Core.
Swagger.
Postman.

Estilos - GET, POST, PUT, DELETE
Artistas - GET, POST
Usuarios - GET

-- Auten/Verifc.

Incluir a autentica��o na aplica��o a fim de que somente ADMINISTRADORES: (estilos musicais)

- Cadastrar [ADM]
- Atualizar [ADM]
- Deletar [ADM]

- A visibilidade para listar os artistas � p�blica. N�o precisa de nenhuma restri��o.

Incluir a autentica��o na aplica��o a fim de que para CADASTRAR artistas:
- Qualquer usu�rio autenticado pode realizar a chamada.  [COMUM]

Por�m, qualquer usu�rio, contanto que esteja autenticado:

- Listar estilos [COMUM]
- Buscar por id. [COMUM]